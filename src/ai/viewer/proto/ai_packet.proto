syntax = "proto3";
import "location_packet.proto";
import "field_packet.proto";


message AIPacket {
    oneof wrapperPacket {
        LocationPacket location =1;
        FieldPacket field = 2;
    }
}
